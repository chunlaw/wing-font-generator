# Wing Font's input TTFs used to live here under Git LFS. They now
# live in a separate repo served via GitHub Pages — see
# python/init_fonts.py for the fetch logic and the rationale (LFS
# bandwidth quota meant 46-slot CI runs ran a $0 open-source project
# into paid-tier territory). This file is intentionally kept (empty
# of LFS rules) rather than deleted so that a stale clone still
# surfaces a clear git status when LFS pointer files linger in the
# working tree.
